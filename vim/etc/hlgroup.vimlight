#
# highlight group definition file
#
# left hand side CamelCase:
#		output from clang
#
# left hand side lowercase_underscore:
#		analyzer's output by analyzing the output from clang
#
# right hand side CamelCase:
#		highlight group in vim.
#		you can see a list of highlight groups by :hi<CR> in vim
#

TypeRef				Type		# type (class/struct/union), excluding builtin types: int, double, etc.
NamespaceRef		Special		# namespace
TemplateRef			Label		# template
OverloadedDeclRef	SignColumn	# unresolved function ref (because of overloading and templating)
VariableRef			Underlined	# variable name in lambda's capture list
error				Error		# error
error_range			ErrorMsg	# error range
init_list_brace		Define		# initializer list's brace
special_function_call	Function	# highlight "func" in "ns::func<type>(arg)"
special_member_call		Function	# highlight "func" in "obj.func<type>(arg)"

# FIXME: not everyone like them?
#operator_call		Label
#member				VisualNOS
#parameter			TabLine

